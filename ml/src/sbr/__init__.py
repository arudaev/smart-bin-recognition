"""Smart Bin Recognition – ML pipeline.

Dataset preparation, auto-labelling, training dispatch and model export for the
two server-side models: the **validator** ("is there a bin?") and the
**identifier** ("which bin?"). See ``docs/04-ml-pipeline.md``.
"""

__version__ = "0.1.0"
