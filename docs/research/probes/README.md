# Probe results

One file per probe from [../../12-validation-protocol.md](../../12-validation-protocol.md),
dated, with the decision rule quoted from the protocol *before* the result.

Empty until the first probe runs.
